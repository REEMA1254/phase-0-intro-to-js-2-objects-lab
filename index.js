const pateEmployeeWithKeyAndValue (employee, key, value) {
    beforeEach(function () {
    for (const key in employee) {
      }
      employee.name = 'Sam';
    });
  } 

  console.log ('employee', function () {
      (updateEmployeeWithKeyAndValue(employee, 'streetAddress', '11 Broadway')).to.eql({
      name: 'Sam',
      streetAddress: '11 Broadway'
    });
  });
